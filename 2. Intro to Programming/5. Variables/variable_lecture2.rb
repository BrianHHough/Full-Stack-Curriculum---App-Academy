# Variables
# the same as: 'count = count + 1' (behaves in same way)

#count = 10
# count += 1 # the same as: 'count = count + 1' (behaves in same way)
#count += 5
#count += 2
#count /= 5
#count -= 3
#count -= 1
#puts count

num = 12 #if it's 13 or an odd number, then it would evaluate false
is_even = num % 2 == 0 # evaluates true
puts is_even
